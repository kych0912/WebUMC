import styled from "styled-components"

const MainContainer = styled.div`
    display:flex;
    gap:8px;
`

const ContentContainer = styled.div`
    display:flex;
    gap:8px;
`

export {
    MainContainer,
    ContentContainer
}